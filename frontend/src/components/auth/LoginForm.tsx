/**
 * LoginForm コンポーネント（スタブ）
 *
 * TDD Red フェーズ: テストが先。このコンポーネントは最小限のスタブとして作成する。
 * 実際の実装はタスク2.4（Green フェーズ）で行う。
 *
 * Requirements: 2.2, 2.7
 */

interface LoginFormProps {
  /** ログイン成功時のコールバック */
  onSuccess: () => void;
}

/**
 * ログインフォームコンポーネント（スタブ）
 * TODO: タスク2.4で実装する
 */
export default function LoginForm(_props: LoginFormProps) {
  // スタブ: タスク2.4で実装
  return null;
}
