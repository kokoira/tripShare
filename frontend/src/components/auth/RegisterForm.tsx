/**
 * RegisterForm コンポーネント（スタブ）
 *
 * TDD Red フェーズ: テストが先。このコンポーネントは最小限のスタブとして作成する。
 * 実際の実装はタスク2.4（Green フェーズ）で行う。
 *
 * Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.7
 */

interface RegisterFormProps {
  /** 登録成功時のコールバック */
  onSuccess: () => void;
}

/**
 * ユーザー登録フォームコンポーネント（スタブ）
 * TODO: タスク2.4で実装する
 */
export default function RegisterForm(_props: RegisterFormProps) {
  // スタブ: タスク2.4で実装
  return null;
}
